

#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;
    while (t--) {
int n;
 cin >> n;
 int count=0 ; 
 for (int i =0 ; i< n ; i ++);
 int x;
 if(x==1){
    count++;
 }
 if (count%2==0)
 cout<<"NO\n";
 else
 
    
 
    }
    return 0;
}
